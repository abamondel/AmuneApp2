<Window x:Class="AmuneApp.Dialogs.FontPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Choose Font" Width="480" Height="520"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" ResizeMode="NoResize">
    <Border CornerRadius="12" Padding="20"
            Background="{DynamicResource SystemChromeMediumColor}"
            BorderBrush="{DynamicResource SystemAccentColorPrimaryBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.3" ShadowDepth="4"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="100"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0" Text="🔤 Choose Font" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

            <!-- Search -->
            <TextBox Grid.Row="1" x:Name="tbSearch" FontSize="14"
                     Margin="0,0,0,8" TextChanged="tbSearch_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Tag" Value="Search fonts..."/>
                    </Style>
                </TextBox.Style>
            </TextBox>

            <!-- Font list -->
            <ListBox Grid.Row="2" x:Name="lbFonts" Margin="0,0,0,8"
                     SelectionChanged="lbFonts_SelectionChanged"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" FontFamily="{Binding}" FontSize="16" Margin="4,2"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Size -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Size:" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="14"/>
                <Slider x:Name="slSize" Minimum="8" Maximum="120" Value="64" Width="280"
                        VerticalAlignment="Center" ValueChanged="slSize_ValueChanged"/>
                <TextBlock x:Name="tbSizeValue" Text="64" VerticalAlignment="Center"
                           Margin="8,0,0,0" FontSize="14" FontWeight="SemiBold" Width="35"/>
            </StackPanel>

            <!-- Preview -->
            <Border Grid.Row="4" CornerRadius="8" Margin="0,0,0,12"
                    BorderBrush="Gray" BorderThickness="1" Padding="10">
                <Viewbox Stretch="Uniform">
                    <TextBlock x:Name="tbPreview" Text="AaBbCc אבגד" FontSize="36"
                               TextAlignment="Center" HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Viewbox>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="OK" Width="100" Appearance="Primary" Click="OK_Click" Margin="0,0,8,0"/>
                <ui:Button Content="Cancel" Width="100" Appearance="Secondary" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
